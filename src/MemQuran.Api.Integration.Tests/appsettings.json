// Any settings in this file will override the default settings in appsettings.json
// But you cannot read this from the test, only applied in the WebApplicationFactory
{
  "Clients": {
    "JsDelivrService": {
      "BaseUrl": "http://localhost:8080"
    }
  },
  "ConnectionStrings:RedisCache": "localhost:6379",
  "OTEL_EXPORTER_OTLP_ENDPOINT": "http://localhost:4317"
}
